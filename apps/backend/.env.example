# Database
DATABASE_URL=postgresql://ventia_user:ventia_password_dev@localhost:5432/ventia_db

# Environment
ENVIRONMENT=development
LOG_LEVEL=info

# CORS
CORS_ORIGINS=["http://localhost:3000"]

# Security (IMPORTANT: Change in production!)  openssl rand -hex 32

SECRET_KEY=your-secret-key-for-encryption-CHANGE-IN-PRODUCTION

# Auth0
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_AUDIENCE=https://your-api-identifier
AUTH0_ISSUER=https://your-tenant.auth0.com/
AUTH0_ALGORITHM=RS256

# eFact-OSE (Facturación Electrónica SUNAT - Perú)
# Base URL del servicio eFact-OSE
EFACT_BASE_URL=https://ose-gw1.efact.pe:443/api-efact-ose
# RUC de Ventia (usado como username en OAuth2)
EFACT_RUC_VENTIA=your_ventia_ruc_here
# Password REST de Ventia para autenticación OAuth2
EFACT_PASSWORD_REST=your_efact_password_here
# Duración del cache del token OAuth2 en horas (eFact tokens duran 12 horas, se recomienda 11)
EFACT_TOKEN_CACHE_HOURS=11

# IMPORTANT NOTES:
# - Shopify credentials are NOT in .env - they are stored per-tenant in the database
# - Each tenant (company) has their own Shopify store credentials in the 'tenants' table
# - Use 'openssl rand -hex 32' to generate SECRET_KEY
# - Get Auth0 public keys from: https://{AUTH0_DOMAIN}/.well-known/jwks.json
# - eFact authentication uses fixed header: Authorization: Basic Y2xpZW50OnNlY3JldA== (Base64 of "client:secret")
# - eFact username is EFACT_RUC_VENTIA, password is EFACT_PASSWORD_REST
# - Each tenant has their own efact_ruc stored in the database for invoice emission

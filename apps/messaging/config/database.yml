default: &default
  adapter: postgresql
  encoding: unicode
  host: <%= ENV.fetch('POSTGRES_HOST', 'localhost') %>
  port: <%= ENV.fetch('POSTGRES_PORT', '5432') %>
  pool: <%= Sidekiq.server? ? ENV.fetch('SIDEKIQ_CONCURRENCY', 10) : ENV.fetch('RAILS_MAX_THREADS', 5) %>
  reaping_frequency: <%= ENV.fetch('DB_POOL_REAPING_FREQUENCY', 30) %>
  schema_search_path: 'messaging,public'
  variables:
    statement_timeout: <%= ENV["POSTGRES_STATEMENT_TIMEOUT"] || "14s" %>

development:
  <<: *default
  database: "<%= ENV.fetch('POSTGRES_DATABASE', 'ventia_db') %>"
  username: "<%= ENV.fetch('POSTGRES_USERNAME', 'ventia_user') %>"
  password: "<%= ENV.fetch('POSTGRES_PASSWORD', 'ventia_password') %>"

test:
  <<: *default
  database: "<%= ENV.fetch('POSTGRES_DATABASE', 'ventia_test') %>"
  username: "<%= ENV.fetch('POSTGRES_USERNAME', 'ventia_user') %>"
  password: "<%= ENV.fetch('POSTGRES_PASSWORD', 'ventia_password') %>"

production:
  <<: *default
  database: "<%= ENV.fetch('POSTGRES_DATABASE') %>"
  username: "<%= ENV.fetch('POSTGRES_USERNAME') %>"
  password: "<%= ENV.fetch('POSTGRES_PASSWORD') %>"
